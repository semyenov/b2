/**
 * UI Constants
 * Configuration for animations, transitions, and UI behavior
 */

/**
 * Animation duration in milliseconds
 * Should match the CSS animation durations in Tailwind config
 */
export const ANIMATION_DURATION_MS = 300
