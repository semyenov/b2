# Docker Environment Configuration Example
# Copy this file to .env before running docker-compose
# DO NOT commit the actual .env file!

# ======================
# Security (REQUIRED)
# ======================
# Generate strong secrets with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here-generate-with-openssl-rand-hex-32
JWT_REFRESH_SECRET=your-refresh-secret-here-generate-with-openssl-rand-hex-32

# ======================
# Database
# ======================
POSTGRES_DB=balda
POSTGRES_USER=balda
POSTGRES_PASSWORD=your-secure-postgres-password

# ======================
# Application
# ======================
NODE_ENV=production
LOG_LEVEL=info

# CORS - Comma-separated list of allowed origins
CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# ======================
# Optional
# ======================
# Port mappings (defaults shown)
# API_PORT=3000
# WEB_PORT=8080
# CADDY_HTTP_PORT=80
# CADDY_HTTPS_PORT=443
